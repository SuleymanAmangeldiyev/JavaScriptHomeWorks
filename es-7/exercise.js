let hello = 'Ciao';     // number
let age = 18;       // boolean
let isGraduated = false;     // string

// (age) will show (NoN : which means Not a Number)on the console, because it isn't a number that's why it cannot be converted to the number... 
console.log(Number(hello));
console.log(Boolean(age));
console.log(String(isGraduated));