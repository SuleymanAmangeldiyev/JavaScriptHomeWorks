// function canPlay() {
//   let sport = " Football";

//   if (true) {
//     let personName = "Cosimo";
//   }
//   console.log(personName + sport);

// }

// canPlay();

function canPlay() {
  let sport = " Football";

  if (true) {
    let personName = "Cosimo";
 // we just need to move this line of code into if ocdes block, becuase which is declared by (let personName = "Cosimo") doesn't effect outside of the block so this is why we get an error 
    console.log(personName + sport);
  }
}

canPlay();
